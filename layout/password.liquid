<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{{ shop.name }}</title>
  {{ content_for_header }}
  {{ 'css/style.css' | asset_url | stylesheet_tag }}
</head>
<body class="bg-black font-sans text-white min-h-screen flex items-center justify-center p-6">
  {{ content_for_layout }}
</body>
</html>
