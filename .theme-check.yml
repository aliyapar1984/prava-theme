# Swiper / GSAP remain on jsDelivr until vendored into assets (large bundles).
RemoteAsset:
  ignore:
    - layout/theme.liquid

# Tailwind Play CDN: senkron yüklenmeli; defer FOUC üretiyor.
ParserBlockingScript:
  ignore:
    - layout/theme.liquid
