# Shopify CLI — önizleme / yükleme için mağaza alan adı
# Kopyalayın: cp .env.example .env  ve değeri doldurun.
# Sonra: npx shopify auth login  (tarayıcıda oturum)
# Ardından: npm run dev

SHOPIFY_FLAG_STORE=ornek.myshopify.com
